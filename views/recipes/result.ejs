<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/styles.css"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lets-Cook App</title>
</head>
<body>
    <div class="container5">
            <div class="button-container">
                <button id="homeButton">Home Page</button>
            </div>
        <script>
            document.getElementById('homeButton').addEventListener('click', function() {
                window.location.href = '/'
            });
        </script>
        </div> 
        <style>
            body { background-color: brown;
                display: flex;
                text-align: center;
                flex-direction: column;
                align-items: center; 
            } 
            h1, h5, h3, h4   {color: beige}
            h2   {color: black}
            h4 {color: brown}
        </style>
        <% if (recipes && recipes.length > 0) { %>
            <% recipes.forEach(function(recipe) { %>
            <h1 class="title"><%= recipe.strMeal %></h1>
            <img src="<%= recipe.strMealThumb %>" alt="recipe image" width="250" height="200">
            <% }); %>
        <% } else { %>
            <h2><%= "Trouble in the kitchen. Try another ingredient." %></h2>
            <img src="/images/ingredients.jpeg" width="250" height="200" style="margin-bottom: 20px;">
            <a href="/recipes/ingredient">Go Back</a>
        <% } %>
    </div>
</body>
</html>